const calculator = (() => {
  const subtract = (a, b) => a - b;
  const divide = () => { };
  const multiply = () => { };

  return {
    subtract,
    divide,
    multiply,
  };
})();

export default calculator;
