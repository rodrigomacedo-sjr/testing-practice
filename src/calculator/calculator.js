const calculator = (() => {
  const subtract = () => { };
  const divide = () => { };
  const multiply = () => { };

  return {
    subtract,
    divide,
    multiply,
  };
})();

export default calculator;
